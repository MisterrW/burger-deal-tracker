<div>
  <h1>Deals for <%=params[:day].capitalize%></h1>
  <% unless @deals_today == nil %>
  <% @deals_today.each do |key, value| %>
  <div margin-top=10px;>
    <h2><%= "#{key}"%></h2>
    <div class="deals">
      <% value.each do |key2, value2| %>
      <div class="deal">
        <h3><%= "#{key2}"%></h3>
        <p>Deal type: <%= "#{value2[0]['details']}" %></p>
        <p>Burgers included:</p>
        <ul>
          <% value2.each do |x| %>
          <li><%= "#{x['burger']}"%></li>
          <% end %>
        </ul>
      </div>
      <% end %>
    </div>
    <% end %>
  </div>
  <% else %>
  <div><p>Sorry, no deals today!</p></div>
  <% end %>


</div>